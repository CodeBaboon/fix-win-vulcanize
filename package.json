{
  "name": "fix-win-vulcanize",
  "version": "1.0.0",
  "description": "Fixes a small issue with pathResolver.js in Vulcanize on Windows command lines",
  "main": "index.js",
  "bin": {
    "fix-win-vulcanize": "index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dave Harris",
  "license": "ISC",
  "dependencies": {
    "command-line-args": "^4.0.1"
  }
}
